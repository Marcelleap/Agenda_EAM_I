<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Agenda_EAM" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorSecondaryVariant">#ffffff</item>
        <item name="color">#ffffff</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#FF6200EE</item> <!-- Cor de fundo do botão -->
        <item name="android:textColor">#FFFFFF</item> <!-- Cor do texto -->
        <item name="android:padding">8dp</item> <!-- Espaçamento interno -->
    </style>

    <style name="Theme.Agenda_EAM" parent="Base.Theme.Agenda_EAM" />
</resources>